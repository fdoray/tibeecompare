blocks:
  - trace
  - punch:
      name: demo
      exec: trace-kit-profile
      begin: tracekit:begin
      end: tracekit:end
      type: kernel
  - current-state
  - sched-wakeup:
      io: yes
      futex: no
  - linux-sched-state
  - execution