blocks:
  - trace
  - punch:
      name: demo
      exec: trace-kit-profi
      begin: tracekit:begin
      end: tracekit:end
      type: ust
  - current-state
  - profiler
  - linux-sched-state
  - execution
